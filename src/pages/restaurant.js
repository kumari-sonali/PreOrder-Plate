import React, { useState } from 'react';
import Navbar from '../navbar/navbar';
import RestaurntData from '../restaurant/restaurant';
const Datapage = () => {
    return (
        <div>
       <Navbar/>
       <RestaurntData/>
       </div>
    );
};

export default Datapage;